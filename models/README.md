# Model files

This folder is reserved for the trained regression model.

The full model file is not stored in the repository by default because of file size limitations.

For full reproduction, place the trained model here:

models/Solovieva_PL_regression_model_v3/
